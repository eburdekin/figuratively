import { Title } from "@mantine/core";

const TimedSessionReview = () => {
  return (
    <div>
      <Title order={2}>Timed Session</Title>
      Review images from session
    </div>
  );
};

export default TimedSessionReview;
